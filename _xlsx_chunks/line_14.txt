R9:  |  | Cloud infrastructure. |  | According to your QA Test Pack, the biggest issue is with the MongoDB selection. Your chat says:  Does the service need stored data or a cache? MongoDB  The AI's architecture plan says: Database: MongoDB (provisioned on OCI) terraform/mongodb.tf MongoDB instance provisioning Managed MongoDB service on OCI  This is the problem.  Why is it wrong? Your QA Test Pack clearly defines what should happen when the user selects "Another service → MongoDB".  Expected behavior: Generate a supported relational database (PostgreSQL stand-in) and add a README note explaining that MongoDB is not directly scaffolded and requires manual configuration. It should not generate a full MongoDB infrastructure.  What the AI did instead  Instead of acknowledging the limitation, it treated MongoDB as fully supported by generating: terraform/mongodb.tf MongoDB instance provisioning Managed MongoDB service MongoDB networking MongoDB IAM configuration MongoDB deployment details  This gives the impression that StackForge fully supports MongoDB infrastructure generation, which is not what the QA pack expects. | Fail | Screenshot (1455).png |  | 
